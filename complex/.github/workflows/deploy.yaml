name: Deploy MultiDocker
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t joelpriddy/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true joelpriddy/react-test npm test

      - run: docker build -t joelpriddy/multi-client ./client
      - run: docker build -t joelpriddy/multi-nginx ./nginx
      - run: docker build -t joelpriddy/multi-server ./server
      - run: docker build -t joelpriddy/multi-worker ./worker

      - run: docker push joelpriddy/multi-client
      - run: docker push joelpriddy/multi-nginx
      - run: docker push joelpriddy/multi-server
      - run: docker push joelpriddy/multi-worker
